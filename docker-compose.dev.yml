
services: 
  backend:
    build:
      context: .
      dockerfile: backend/docker/dockerfile.dev
    container_name: arkanoid_back
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app/backend
    env_file:
      - ./backend/.env
    command: npm run start:dev-watch